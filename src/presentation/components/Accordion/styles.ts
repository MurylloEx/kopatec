import styled from 'styled-components';

const Container = styled.div`
  -webkit-box-shadow: 10px 15px 15px -18px rgba(0,0,0,0.3);
  -moz-box-shadow: 10px 15px 15px -18px rgba(0,0,0,0.3);
  box-shadow: 10px 15px 15px -18px rgba(0,0,0,0.3);
`;

export const Styled = {
  Container,
}
